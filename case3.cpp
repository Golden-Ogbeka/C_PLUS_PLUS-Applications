#include<iostream>
using namespace std;
Point(negaative(),distance());
int negate(int){
	int x;
	x=-(x);
}
int distance(int,int,double){
	int a,b,c;
	c=((a^2)+(b^2))^(0.5);
	return 0;
}

class Point{
	int x,y;
	public:
		negate();
		distance();
		
};


int main(){
	int x,y,a,b;
	cout<<"Input a value for x: "<<"\n";
	cin>>x;
	cout<<"Input a value for y: "<<"\n";
	cin>>y;
	Point x;
	Point y;
}